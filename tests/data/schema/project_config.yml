---
general:
  project_root: ../
project:
  owner: owner@company.com
  name: nested-fail
  regions:
    - us-east-1
    - us-west-2
tests:
  taskcat-json:
    parameter_input: ci/input.json
    template_file: templates/test.template.yaml
    parameters:
      MyParam: MyValue